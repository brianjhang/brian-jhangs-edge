---
import { getCollection } from "astro:content";

// 讀 daily collection
const posts = await getCollection("daily");
---

<h1>今日精選</h1>

<ul>
  {posts.map(post => (
    <li>
      <a href={`/daily/${post.slug}`}>{post.data.title}</a>
    </li>
  ))}
</ul>
